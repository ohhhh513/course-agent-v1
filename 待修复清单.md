# 待修复清单（已核实 · 先登记不修）

> 用途：登记**已经查证、但本轮约定先不动**的问题，避免只在口头/截图里流失。
> 记录规范：修完把 `[ ]` 改成 `[x]` 并注明日期与提交（或直接删掉该条）。
> 与 `最新bug修改9.23.md` 的分工：那份是**改动记录**（已经改了什么），本文件是**未修问题清单**（还没改什么）。
> 最后更新：2026-09-24

---

## 怎么读这份清单（写给不熟悉本项目的同学）

每条固定三段，先看这三段就能判断"要不要管、找谁管"：

- **归属**：这是**前端**、**后端**、还是**数据库表结构 / 文档仓库**的问题；后面附具体文件或接口，谁认领一看就知道该动哪里。跨两边的写「前端 + 后端」。
- **界面**：在哪个界面、点什么能看到它（按 端 → 分组 → 页面 的顺序写）。写「无」的是内部/接口层问题，正常操作看不到，只有直接调接口或看日志才会遇到。
- **影响**：现在实际会发生什么、影响谁。**「当前数据下无可见影响」会明确写出来** —— 这类是「潜在坑」（数据一变就会暴露），不是正在发生的错误。

两端界面导航名（下文提到的一律以此为准）：

- **学生端**：学习驾驶舱 / 课程图谱导航 / 学习资源中心 / AI 智能答疑 / 智能练习 / 学情中心 → 我的学情 / 学情中心 → 预警与提醒
- **教师端**：教学概览 → 教学驾驶舱 / 教学概览 → 学情监测看板 / 精准分析 → 归因与错题分析 / 精准分析 → AI 出题与题库 / 学情产出 → 学情分析报告 / 课程 → 课程目录与资源 / 课程 → 知识图谱编排
- ⚠️ 教师端「教学干预与策略」已于 2026-09-24 从界面移除（后端接口仍在），所以涉及它的条目现在**无界面可见**。

---

## 速查表（P1–P43）

| 编号 | 问题一句话 | 归属 | 在哪看 | 影响 |
| --- | --- | --- | --- | --- |
| P1 | 目标图谱/能力雷达没实现 | 后端（前端卡片已删） | 原「我的学情」雷达卡，已移除 | 低：展示已删；后端留死接口 |
| P2 | `GOAL_KP_MAP` 仍是旧编号 | 后端 | 无 | 低（潜在）：补目标图谱后才会犯 |
| P3 | 问题/目标图谱无创建入口 | 后端（前端页签已删） | 原「课程图谱导航」页签，已移除 | 中：这两类图谱永远空 |
| P4 | 归因分析未按课程过滤 | 后端 | 教师端·归因与错题分析 | 低（潜在）：多课程时统计串课 |
| P5 | 归因阈值是 v1.0 定值 | 后端 | 教师端·归因与错题分析 | 中：可能误判/漏判 |
| P6 | 同一接口两种响应结构 | 后端 | 无（前端已兜底） | 低：改前端时容易踩 |
| P7 | 「主要错项」是猜的 | 后端 | 教师端·归因与错题分析 | 中：误导教师判断 |
| P8 | 错误类型字段没有真实来源 | 后端 | 教师端·归因与错题分析 | 中：该维度天然没数据 |
| P9 | 学生端接口未按课程过滤 | 后端（**大部分已修**） | 学生端·我的学情 / 预警与提醒 | 低：剩余项前端已不调用 |
| P10 | 三个占位接口假装成功 | 后端（前端仍有入口） | 教师端·AI 出题与题库 | 中：看着成功、其实没产物 |
| P11 | 干预「对照组」写死 +8 | 后端 | 原教师端·教学干预与策略（已隐藏） | 低：现在无界面 |
| P12 | 干预模板成效是编的 | 后端 | 同上 | 低：现在无界面 |
| P13 | `/ai/suggest-questions` 查空表 | 后端 | 原「猜你想问」旧接口，已弃用 | 低：已绕开 |
| P14 | 退出登录不吊销 token | 后端 | 三端右上角「退出登录」 | **高**：共用电脑有安全风险 |
| P15 | 两个归因端点没人调用 | 后端 + 前端 | 无 | 低：冗余 |
| P16 | 别名端点 | 后端 | 无 | 低：确认没人误用即可 |
| P17 | 题库/报告接口无课程归属校验 | 后端 | 无（靠调接口越权） | **高**：跨课程读改 |
| P18 | 9 个模型残留演示课默认值 | 后端 + 数据 | 无（触发时是 500） | 中：漏传课程即报错 |
| P19 | 教师端 `resource.js` 是死文件 | 前端 | 无 | 低：改它没用，浪费工时 |
| P20 | 驾驶舱待办「去处理」点了没反应 | 前端 | 教师端·教学驾驶舱 | 中：断链 |
| P21 | 环形图写死「共 46 个」 | 前端 | 学生端·学习驾驶舱 | 低：数字错（实际 32） |
| P22 | `api.js` mock 死代码 | 前端 | 无 | 低：维护干扰 |
| P23 | 登录页提示演示账号 | 前端 | 登录页 / `start.bat` | 中：按提示登录会失败 |
| P24 | 「能力目标增益」命名不实 | 前端 | 学生端·智能练习 → 练习报告 | 低：误导命名 |
| P25 | 首页文案提已下线功能 | 前端 | 登录页左侧 / 学生首页 | 低：宣传了不存在的功能 |
| P26 | 注释仍写「三大图谱」 | 前端注释 | 无 | 低：纯误导 |
| P27 | Git 跟踪大文件/临时文件 | 仓库 | 无 | 中：仓库臃肿 |
| P28 | 垃圾文件 | 仓库 | 无 | 低 |
| P29 | README 等文档口径过时 | 文档 | 无 | 中：新人照它上手会走错 |
| P30 | PPT/PDF 进度是学生自报 | 前端 + 后端 | 学生端·学习资源中心 → 打开 PDF/PPT | 中：进度可自填、只增不减 |
| P31 | PDF 总页数解析可能不准 | 后端 | 学生端·学习资源中心 → PDF 进度 | 中：百分比偏小或恒 0 |
| P32 | 自报进度喂给掌握率 | 后端 | 学生端·课程图谱导航 / 学习路径 | **高**：不答题也能显示"已掌握" |
| P33 | PDF/PPT 阅读时长不计入 | 后端 | 学生端·学习驾驶舱 → 今日学习时长 | 中：看一小时文档时长是 0 |
| P34 | 「累计阅读 N 页」是页码求和 | 后端 | 学生端·我的学情 → 资源学习进度 | 低：数字虚高 |
| P35 | 视频完成度可被拖动绕过 | 前端 + 后端 | 学生端·学习资源中心 → 视频播放器 | 中：完成度不可全信（时长可信） |
| P36 | 「班级对比定位」名字与口径不符 | 前端 | 学生端·我的学情 | 低：口径已是课程内 |
| P37 | 教师复核预警必然 403 | 后端 | 教师端·学情监测看板 → 异常预警列表 | ✅ 已修 |
| P38 | 教师端路由没有角色校验 | 后端 | 无（靠调接口越权） | **高**：学生可读教师数据 |
| P39 | 教师端多处查询没按课程收敛 | 后端 | 教师端·驾驶舱 / 学情监测看板 / 归因分析 | 中：多课程时数字串课 |
| P40 | 打卡/资源时长表缺课程字段 | 数据（表结构） | 学生端·学习驾驶舱 → 学习天数/时长 | 中：切课仍合并统计 |
| P41 | 练习模式文案与实现不符 | 前端 + 后端 | 学生端·智能练习 → 模式卡片 | 中：承诺的功能没实现 |
| P42 | 归因分权重口径 | 后端 | 学生端·我的学情 / 预警、教师端预警 | ✅ 已实施 |
| P43 | 统计字典靠 KP id 全局唯一 | 后端（架构前提） | 无（当前结果正确） | 低（潜在）：撞号会静默混课 |

---

## 一、图谱相关

### [ ] P1 · 目标图谱 / 能力目标达成度没实现 —— **前端卡片已删（2026-09-24）**

- **归属**：后端 · `backend/app/routers/student.py:906-913`（`_GOAL_DIMENSIONS`）、接口 `/student/ability/radar`；前端卡片已删除。
- **界面**：原本在 **学生端 → 学情中心 → 我的学情** 的「能力目标达成度」雷达卡（2026-09-24 已移除）；后端接口仍在但前端已无人调用。
- **影响**：学生看不到这块内容了，也就看不到"全是 0 的图"。后端接口与维度映射成了死代码，留着会让人以为这功能还在。
- 细节：`_GOAL_DIMENSIONS` 里 6 个能力维度映射的知识点 id 仍是 v1.0 编号（`KP11/KP12/…/KP44`）。实测当前课程知识点为 `KP001…KP032`，`learning_paths` 里 `KP0xx` 32 条、`KP1x/KP2x` **0 条** → `kp in lp_map` 永不成立 → 6 个维度恒 0，雷达图里只有硬编码的「目标基线 `[80,80,80,80,75,75]`」有值。
- 根因：**目标图谱本身没有实现**（见 P3）。附带：`/student/ability/radar` 未按课程过滤（`LearningPath.user_id == uid`、按 `class_name` 找同班），属多课程隔离缺口。
- ⚠️ 另有两处**同名但数据真实**的指标仍在用，本次未动（待定：改名 or 删）——**学生端 → 学习驾驶舱** 的环形图（`dashboard.js:178`）与**学生端 → 我的学情** 顶部第三个指标（`mastery.js:61`）。两处数值来自 `goalAchieveRate = 达标知识点数(≥60%) / 总知识点数`（真实计算），但标签写成「能力目标达成度」、提示写死「目标基线 80%」，名字指向不存在的目标图谱。

### [ ] P2 · `GOAL_KP_MAP` 陈旧（目标图谱达成度恒 0）

- **归属**：后端 · `backend/app/routers/graph.py:24`。
- **界面**：无（目标图谱在前端已无入口）。
- **影响**：现在无可见影响。等哪天补上目标图谱节点，达成度**仍然恒为 0** —— 典型的"修了上游、忘了下游"的坑。
- 细节：value 仍是 `KP01/KP11/…`，`_goal_achieve()` 查不到知识点。

### [ ] P3 · 问题图谱 / 目标图谱缺创建入口

- **归属**：后端 · `backend/app/routers/graph.py`（接口有、创建入口没有）；前端页签已删除。
- **界面**：原本在 **学生端 → 课程图谱导航** 的两个页签（已删）；**教师端 → 课程 → 知识图谱编排** 只能建「章 / 知识点」。
- **影响**：这两类图谱**永远不会产生真实数据**；接口留着，若有人直接调用只会拿到空数据或已废弃的演示数据，容易误判成"功能做好了"。

---

## 二、归因 / 学情统计口径

### [ ] P4 · 归因分析未按课程过滤（`analysis_errors`）

- **归属**：后端 · `backend/app/routers/teacher.py:1054`（`AnswerRecord`）、`:1096`（`LearningPath`）。
- **界面**：**教师端 → 精准分析 → 归因与错题分析**（前提：有学生同时选了两门以上课程）。
- **影响**：会把**别的课程**的答题/学习路径算进来，教师看到的错题分布与薄弱点排名会偏。**当前数据下无可见影响**（另一门课全是 `todo`，被 `status != 'todo'` 挡掉了）→ 属潜在坑：那门课一旦有真实作答就会串。
- 细节：这两处只按 `user_id.in_(student_ids)` 过滤、未带 `course_id`；而同函数的 `chapter` 分支却过滤了课程 → 同函数内口径不一致。实测 `Ss` 同时选两门课时 `class_lps` 取到 32 行（含另一门课），加过滤应为 26 行。

### [ ] P5 · 归因判定阈值是 v1.0 时代的定值

- **归属**：后端 · `backend/app/routers/teacher.py` 归因分析的阈值常量（**不是本次改过的逻辑，是原始实现**）。
- **界面**：**教师端 → 精准分析 → 归因与错题分析** 的「共性薄弱 / 个体异常」两块。
- **影响**：阈值诞生于班级维时代、此后**从未复核**，可能把正常学生判成"个体异常"，也可能漏掉真问题。**需要产品侧重新拍阈值**，不是纯技术修复。
- 细节：共性薄弱 `ratio>=30% 且 affected>=2`；个体异常 `有效学习点>=3 且 avg<40% 或 完成率<30%`。诞生于 `2655147`（2026-09-05），`8d84169`（09-15）只把数据来源从班级改成课程。

### [ ] P6 · 同一接口返回两种结构（`analysis_errors`）

- **归属**：后端 · `teacher.py::analysis_errors`。
- **界面**：无直接可见（对应 **教师端 → 精准分析 → 归因与错题分析** 的"无学生"情形）。
- **影响**：低。前端目前用 `Array.isArray(d.topWrongQuestions) ? … : []` 兜底，不会崩；但日后前端若改去读 `total/wrongRate/errorTypes`，在"无学生"分支会拿到 `undefined`。
- 细节：「无学生」早退分支返回 `total/wrongRate/errorTypes/mainWrongOption/topWrong/…`；正常分支返回 `scope/topWrongQuestions/weakChain/…` —— 两套字段名不一样。

### [ ] P7 · 「主要错项」是猜出来的

- **归属**：后端 · `teacher.py:1089`。
- **界面**：**教师端 → 精准分析 → 归因与错题分析** → 错题排行里的「主要错项」列。
- **影响**：显示的错误选项不是统计结果，而是 `"B" if wc > t*0.6 else "C"` 这个启发式猜的 → 教师据此判断"学生都错选了 B"可能被误导。

### [ ] P8 · 错误类型字段没有真实来源

- **归属**：后端 · 写入路径 `practice.submit_answer`（答错时抄题目的 `error_type`）。
- **界面**：**教师端 → 精准分析 → 归因与错题分析** 的错误类型维度。
- **影响**：题库该字段**普遍为空**（本地 229 题全空）→ 错误类型统计天生没数据，归因类功能缺输入。要真做起来，得先有标注错误类型的入口。

### [ ] P9 · 学生端接口未按课程过滤（**大部分已修**）

- **归属**：后端。
- **界面**：**学生端 → 我的学情 / 预警与提醒**（切课后对比数字是否变化即可看出）。
- **影响**：已修部分不再串课；剩余项前端已不调用，当前无可见影响。
- 已修（2026-09-24）：`/mastery/matrix`、`/growth`、`/compare`（第十八节）；`/alerts` 与驾驶舱 `active_alerts`（第二十节）；随机练习抽题范围（第二十八节）。
- 前端已卸载：`/ability/radar` 不再被调用（第二十九节）。注意：**正确率的数值口径本身没有跨课程问题**（第二十八节"口径澄清"），那次修的是"范围选错"。

---

## 三、接口占位与伪数据

- [ ] **P10 · 三个接口是占位实现、但前端仍有入口**
  - **归属**：后端 · `/question/materials`（返回假 fileId）、`/question/packs`（返回假 packId + `pushed:true`）、`/ai/feedback`（空实现）。
  - **界面**：**教师端 → 精准分析 → AI 出题与题库**（资料 / 题包相关动作）；**学生端 → AI 智能答疑** 的反馈按钮。
  - **影响**：操作**看着成功，实际没有任何真实产物**（没有真题包、没有真反馈记录），容易让人以为功能可用。
- [ ] **P11 · 干预「对照组」是写死的 +8**
  - **归属**：后端 · `/intervention/{iv_id}/effect`。
  - **界面**：原「教师端 → 教学干预与策略」（2026-09-24 已隐藏）→ 现在**无界面可见**。
  - **影响**：当前无用户可见影响；接口仍会返回编造的对照组数值，若日后恢复入口就会直接骗人。
- [ ] **P12 · 干预模板成效是编的公式**
  - **归属**：后端 · `/intervention/templates` 的 `successRate` / `avgLift`。
  - **界面**：同 P11（已隐藏，无界面可见）。
  - **影响**：同 P11。
- [ ] **P13 · `/ai/suggest-questions` 查的是永空表**
  - **归属**：后端（接口）+ 前端（已弃用）。
  - **界面**：原用于 **学生端 → AI 智能答疑 → 猜你想问**；现已改用 `/student/dashboard` 的 `suggestedQuestions`。
  - **影响**：无可见影响（已绕开）；风险是后来的人以为它还能用。
- [ ] **P14 · 退出登录不吊销 token**
  - **归属**：后端 · `/auth/logout`（只返回成功，不吊销）。
  - **界面**：三端右上角 **退出登录**。
  - **影响**：**安全风险（本清单最高的一档）**：退出只是前端清了本地 token，旧 token 在 7 天有效期内仍可用 → 共用电脑上换人登录后，前一个人的身份仍能被使用。
- [ ] **P15 · 两个归因端点没人调用**
  - **归属**：后端 + 前端（前端从未调用）。
  - **界面**：无。
  - **影响**：无用户可见影响；`/analysis/causes`、`/analysis/weak-chain` 属冗余端点，建议随文档一起清掉（别再对外宣传）。
- [ ] **P16 · 别名端点**
  - **归属**：后端 · `/ai/chat` 与 `/question/gen` 的 `/gen/stream`。
  - **界面**：无。
  - **影响**：无；只需确认没有页面/脚本误用旧别名。

---

## 四、隔离与权限

- [ ] **P17 · 题库/报告接口无课程归属校验**
  - **归属**：后端 · `PUT/DELETE /question/{qId}`、`POST /question/review`、`GET /report/{reportId}`。
  - **界面**：无界面路径 —— 属于"直接调接口就能越权"。
  - **影响**：**安全风险**：拿到别门课的 `qId` / `reportId` 就能跨课程改题、读报告。
- [ ] **P18 · 9 个模型残留演示课默认值**
  - **归属**：后端 + 数据 · `Alert/Resource/GraphNode/GraphLink/KpDetail/LearningPath/PracticeSession/AnswerRecord/Question` 都还写着 `default="C2026DS001"`。
  - **界面**：无（触发时表现为 500 报错）。
  - **影响**：某处漏传 `course_id` 时，写入会因外键指向不存在的演示课而失败 → 表现为"操作报错"，且很难看出是课程归属问题。
- [x] **P37 · 教师复核预警必然 403** —— **已修（2026-09-24）**
  - **归属**：后端 · `teacher.py:938-953`（`review_alert`）→ 已改为按"预警属于当前课程 + 调用者在该课程是教师"校验，班级维不再参与鉴权。
  - **界面**：**教师端 → 教学概览 → 学情监测看板 → 异常预警列表 → 复核预警**。
  - **影响**：修复前点「确认」「忽略」**必然 403**（后端拿班级维比对，而班级表是空的），预警永远无法复核；现已修（第二十一节，需重启后端生效）。
- [ ] **P38 · 教师端路由没有角色校验**
  - **归属**：后端 · 所有 `/teacher/*`、`/analysis/*`、`/question/*`、`/intervention/*`、`/report/*`。
  - **界面**：无界面路径 —— 属于"用学生账号的 token 直接调教师接口"。
  - **影响**：**安全风险**：学生账号能读到课程级的教师端数据（全班预警、学情画像、题库统计）。参照 `routers/st_agent.py` 已有 `_require_teacher`，这里缺。
  - 建议：加 `require_teacher` 依赖并挂到教师端路由（或在 router 层统一 `dependencies=[...]`）。属横切改动，需先评估对现有前端的影响。
- [ ] **P39 · 教师端多处查询没按内容自身的课程收敛**
  - **归属**：后端 · `teacher.py:211`（今日完成的 `PracticeSession`）、`:220`（今日 `AnswerRecord`）、`:497`（学生最近活跃）、`:688`（个体画像答题明细）、`:810`（活跃趋势时长）、`:900`（预警趋势最近 5 次作答）、`:1069`（归因答题集）、`:1238`（薄弱链路）、`:1274`（成因分类）、`:1743`（题库正确率聚合）。
  - **界面**：**教师端 → 教学驾驶舱**（今日活跃 / 提交次数）、**→ 学情监测看板**（个体画像、活跃趋势）、**→ 归因与错题分析**。
  - **影响**：学生同时选多门课时，这些数字会把别课数据算进来（偏大、串课）。当前只有 1 名学生同时选两门课、且另一门课基本没有数据，所以**影响面小但确实存在**。面较大，建议单独排一轮统一补 `course_id`。
- [ ] **P40 · 打卡/资源时长表缺课程字段（无法按课程隔离）**
  - **归属**：**数据（表结构）** · `study_checkins`（只有 `user_id + day`）、`resource_study_log`（只有 `user_id + day`）→ 要真正隔离必须加 `course_id` 列。
  - **界面**：**学生端 → 学习驾驶舱** 的「连续/累计学习天数」与「今日累计学习时长」。
  - **影响**：切课后这些数字仍是**两门课合并**的口径（练习部分已按课程隔离，资源/打卡部分没有）。
  - ⚠️ 按约定**不改数据库格式**，先登记待议；修它要连带决定这两张表怎么迁移。
- [ ] **P41 · 练习模式文案与实际实现不符**
  - **归属**：前端文案 + 后端实现。
  - **界面**：**学生端 → 智能练习** → 模式卡片（顺序练习 / 薄弱点强化）。
  - **影响**：两处 ——
    1. 「顺序练习」写「按章节与知识点**前后置顺序**逐题推进」：后端**从未**按 `graph_links(relation='pre')` 排序（本课关系边 **0 条**、`kp_details.pre_kp` 全空），现在是「知识点顺序 → 做错过 → 难度」排 —— 文案是空头承诺。
    2. 「薄弱点强化」卡片题量按 `min(10, 全课题数)` 算，未按"薄弱点题池"算 → 卡片写 10 题、实际可能只有 3 题（组卷完成时的提示会显示真实题量）。
  - 注：「随机练习」的同类问题已于 2026-09-24 修好（第二十八节），**只剩「薄弱点强化」这一张卡没对齐**。修它要在 `practice_modes` 里按 `ENGAGE_MIN + <60%` 现算薄弱点（现在这份名单是驾驶舱算完由前端回传的）。
- [x] **P42 · 归因「分权重」口径** —— **已实施（2026-09-24）**
  - **归属**：后端 · `services/scoring.quiz_accuracy_by_kp`（全站统一入口）。
  - **界面**：**学生端 → 我的学情 / 预警与提醒**、**教师端 → 学情监测看板** 的预警等级与文案。
  - **影响**：口径已统一为主 KP 权重 **1.0**、其它标签 **0.3**；「不同题数」（预警门槛用）**含次要命中**；预警阈值与门槛数值不变。结构字段没变，只有数值变。
- [ ] **P43 · 统计字典靠「KP id 全局唯一」做课程隔离（架构前提，非当前 bug）**
  - **归属**：后端 · `services/scoring.quiz_accuracy_by_kp(db, user_id)`、`services/kp_stats.mastery_by_kp(db, user_id)`（两者都**没有 course 参数**）。
  - **界面**：无（当前结果正确，看不到异常）。
  - **影响**：**当前无影响** —— 这几处返回的是"该生所有课程的 KP 字典"，但消费方都是「课程内的知识点列表 × `.get(kp_id)`」，所以取到的仍是本课程的。风险在于它**依赖 KP id 全局唯一**：表约束只有 `(id, graph_type)` 联合主键、**不含 `course_id`**，DB 层并不禁止两门课用同一个 KP 号。一旦出现同号（手工导库、按课程重新编号、并行导入），正确率 / 掌握率 / 薄弱点 / 预警会**一起静默算错且不报错**。目前由 `teacher.py::_next_graph_id()`（扫描全表取 `max+1`）保证唯一，实测 32 节点 0 撞号。
  - 为什么"当前是对的"：三类消费方都自带课程范围 —— 驾驶舱薄弱点走 `LearningPath.course_id == 当前课程`（`student.py:167-170`）、知识图谱走 `graph_nodes.course_id`、预警按知识点自身的 `course_id` 落库；库副本实测「只取该课程作答记录重算」与全量口径**逐点一致**。
  - 建议（未做，需先议）：给这些函数补**可选** `course_id` 参数（默认 `None` = 现状，逐一改 4 个消费方），或在 KP 创建/导入路径上强制"先查全库是否已存在该 id"。

---

## 五、前端残留

- [ ] **P19 · 教师端 `resource.js` 是死文件**
  - **归属**：前端 · `assets/js/teacher/views/resource.js`（`teacher.html` 未引入，且 `'resource'` 路由被 `structure.js` 覆盖）。
  - **界面**：无。**教师端 → 课程 → 课程目录与资源** 实际由 `structure.js` 渲染。
  - **影响**：改这个文件**不会有任何效果**，最容易浪费后来同学的时间。
- [ ] **P20 · 教师端驾驶舱待办的「去处理」点了没反应**
  - **归属**：前端（待办 `target='alerts'` / `'students'` 没有对应视图）。
  - **界面**：**教师端 → 教学概览 → 教学驾驶舱** → 待办列表。
  - **影响**：点击后页面无变化，用户会以为坏了。
- [ ] **P21 · 学生首页环形图写死「共 46 个」**
  - **归属**：前端 · `assets/js/student/views/dashboard.js`。
  - **界面**：**学生端 → 学习驾驶舱** 的环形图。
  - **影响**：数字错（实际 32 个知识点），与「我的学情」页对不上。
- [ ] **P22 · `api.js` mock 死代码与死接口**
  - **归属**：前端 · `assets/js/api.js`（约 200 行 mock + 无人调用的定义：`analysis.weakChain/causes`、`ai.agentStatus`、`student.abilityRadar`）。
  - **界面**：无。
  - **影响**：无功能影响，但**维护干扰**；若哪天把 `API.config.mode` 切回 `mock`，页面会显示过时的假数据。
- [ ] **P23 · 登录页仍在提示演示账号**
  - **归属**：前端/脚本 · `start.bat` 启动横幅、`index.html` 的 `DEMO_HINT`。
  - **界面**：**登录页**（`index.html`）与启动时的控制台输出。
  - **影响**：按提示的 `student/123456` 登录**会失败**（现在没有演示账号），新人上手第一步就卡住。
- [ ] **P24 · 练习报告「能力目标增益」命名不实**
  - **归属**：前端文案（数值来自后端练习报告 `scoreGain`）。
  - **界面**：**学生端 → 智能练习** → 练习报告卡片。
  - **影响**：名字暗示"目标图谱增益"，实际是**本次答对题目的分值合计**，与目标图谱无关。（同卡的「已回写目标图谱达成度」提示已于 2026-09-24 删除。）
- [ ] **P25 · 首页/登录页文案提到已下线功能**
  - **归属**：前端 · `index.html` 左侧功能列表第 3 条「AI 自动归因与干预策略推荐」、`home.html:37`「通过三大图谱自主导航学习」、`home.html:29` 写死演示课程徽标。
  - **界面**：**登录页**左侧、**学生首页**（`home.html`）。
  - **影响**：对外宣传了已经下线/隐藏的功能，容易被当成"功能缺失"。
- [ ] **P26 · 代码注释仍写「三大图谱」**
  - **归属**：前端注释 · `assets/js/charts.js:200` 等处。
  - **界面**：无。
  - **影响**：无功能影响，纯误导后来的人。
- [ ] **P36 · 「班级对比定位」名字与口径不符**
  - **归属**：前端文案（后端口径已于 2026-09-24 改为「当前课程成员」，与教师端同源）。
  - **界面**：**学生端 → 学情中心 → 我的学情** →「班级对比定位」卡片（含右侧「班级第 1/1 名」徽标）。
  - **影响**：现在比的是**课程内成员**，不是行政班；名字会让人以为按班级比。待定是否改名（如「课程内对比」）。

---

## 六、仓库卫生与文档

- [ ] **P27 · Git 跟踪大文件与临时文件**
  - **归属**：仓库 · `backend/app/data/st/rag.db`(28MB)、6 个 `course_agent.db.bak.*`、`backend/data/.pytest-tmp-*`、`resource_registry_snapshot.json`。
  - **界面**：无。
  - **影响**：仓库臃肿、clone 慢，且数据库文件本不该进版本库（容易覆盖别人的数据）。`.gitignore` 已写好，`git rm --cached` **待办未执行**。
- [ ] **P28 · 垃圾文件**
  - **归属**：仓库 · `assets/js/teacher/views/111.txt`、`backend/app/111.txt`（内容都是 `Ciallo～(∠・ω<)⌒☆`）、根目录空文件 `1.55.3`。
  - **界面**：无。
  - **影响**：无功能影响，但容易被误当成年久失修的正常文件。
- [ ] **P29 · 文档口径过时**
  - **归属**：文档 · 根 `README.md`、`docs/README.md`、`docs/项目真实交互梳理.md`。
  - **界面**：无。
  - **影响**：基线停在 2026-09-11（演示号 / `C2026DS001` / 12 名学生），与现行"多课程、无演示账号"不符 → 新人照着上手会走错。

---

## 七、资源进度与完成率（**只登记，先放着**）

> 背景与结论：这一组是「实现有毛病，但**现在不能删**」的东西 —— PPT/PDF 的百分比与 `resource_progress`、资源完成率，以及由完成率喂出来的掌握率是**耦合**的。删掉百分比或统计会让资源完成率失真，因此**保持现状、不改逻辑**，仅登记待日后一并设计。
> 注：2026-09-24 已在**显示层**移除资源卡的进度条（第十七节），但数据、接口与百分比**均未改动**。
> **已决策（2026-09-24）**：百分比与相关统计**保留**，不做删除。修这组问题时必须连带考虑资源完成率与掌握率，属于"口径变更"，需先出影响分析再动。

- [ ] **P30 · PPT/PDF 的「进度」是学生自报页码，不是测量**
  - **归属**：前端（弹窗收集页码）+ 后端（拿页码算百分比）· 前端 `assets/js/student/views/resource.js:797`、`:842`；后端 `backend/app/routers/student.py:634-635`、`:647`。
  - **界面**：**学生端 → 学习资源中心** → 打开 PDF（「跳转页码」输入框）或 PPT（「已读到第 N 页 + 保存进度」）。
  - **影响**：进度可以**自己填**，且 `p.progress = max(prev, 新值)` **只增不减** —— 填到最后一页就永久 100%，填错了也改不回来。PPT 走浏览器内置阅读器/下载，前端技术上拿不到翻页事件。
- [ ] **P31 · PDF 总页数解析可能不准**
  - **归属**：后端 · `backend/app/media_utils.py::pdf_pages()`。
  - **界面**：**学生端 → 学习资源中心** → PDF 资源的进度百分比。
  - **影响**：解析偏小时百分比虚低；解析为 0 时该资源**进度恒为 0**。（`pptx_pages()` 数幻灯片文件，可靠。）
- [ ] **P32 · 自报进度会喂给「掌握率」，进而影响图谱与路径状态**
  - **归属**：后端 · `services/kp_stats.py:35`、`routers/graph.py:430`。**这条是本组"不能随便删"的主因。**
  - **界面**：**学生端 → 课程图谱导航**（节点变绿「已掌握」）、**我的学情 / 学习路径**（显示「已完成」）。
  - **影响**：**不做任何题**，只要把 PPT 页码填满，该知识点就会显示"已掌握"。预警不受影响（预警只看答题记录）。这也是删百分比/统计会让资源完成率失真的原因。
- [ ] **P33 · PPT/PDF 阅读时长完全不计入学习时长**
  - **归属**：后端 · `resource_study_log` 只在视频分支写入（`student.py:654-663`）；`_today_study_seconds` 只累加「练习用时 + 视频观看秒数」。
  - **界面**：**学生端 → 学习驾驶舱** 的「今日累计学习时长」。
  - **影响**：看一小时 PDF/PPT，学习时长显示 **0**（学生会觉得"白学了"）。
- [ ] **P34 · 「累计阅读 N 页」口径是页码求和**
  - **归属**：后端 · `student.py:789`（`read_pages += position`）。
  - **界面**：**学生端 → 我的学情** →「资源学习进度」卡里的「累计阅读」。
  - **影响**：是各文档「上次看到的页码」之和，不是"读了多少页"：同一份 100 页 PPT 看到第 50 页记 50，换一份继续累加 → 数字虚高。
- [ ] **P35 · 视频完成度可被拖动绕过（但时长可信）**
  - **归属**：前端（播放器事件）+ 后端（累计口径）。
  - **界面**：**学生端 → 学习资源中心** → 视频播放器。
  - **影响**：拖到最后触发 `ended` → `progress = 100`；`watchedDelta` 只累计真实播放的正向增量，所以口径是「**时长可信、完成度不可全信**」。

---

*登记方式：本轮所有条目均经代码通读或数据库实测确认；标「实测」的附有可复现的数字。*
*每条开头的三行（归属 / 界面 / 影响）是 2026-09-24 为跨成员阅读补的说明，技术细节仍保留在其后。*
