# ============================================================
#  ⚠️ 本文件已废弃 —— 配置模板已迁移至 backend/app/.env.example
# ------------------------------------------------------------
#  实际加载位置：backend/app/.env（与 config.py 同目录，
#  由 app/config.py 的 env_file 第一项指定，与启动目录无关）。
#  请勿在本目录创建 .env，以免与 app/.env 混淆产生双份配置。
# ============================================================
