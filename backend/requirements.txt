fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2
sse-starlette==2.1.3 # compat: 2.2.x requires starlette>=0.41.3, conflicting with fastapi 0.115 (<0.39)
starlette==0.38.6   # pin: keep fastapi 0.115 happy (it caps starlette at <0.39)
bcrypt==4.0.1       # passlib[bcrypt] compat: keep the working bcrypt version
# ===== agent_st (question bank / intelligent parse / question generation) =====
# note: openai < 1.55.3 is incompatible with httpx >= 0.28 (proxies TypeError), pin lower bound
openai>=1.55.3,<2
httpx>=0.27,<0.29
PyYAML==6.0.2
pypdf==5.1.0
