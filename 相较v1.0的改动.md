# 相较 v1.0 的改动记录

> 项目级约束：本项目为多人协同开发。自本文档建立之日起，**任何对项目的改动都必须按块（后端 / 前端渲染 / LLM·Agent / 启动脚本·文档 / 数据库·数据 等）分条记录在本文档中**，每条尽量简短但务必准确，注明涉及文件与改动动机。
> 记录时间：2026-09-06（设备更换后的首次检查与修复）

---

## 一、启动脚本与文档（Python 解释器 3.12 → 3.11）

- `start.bat`：删除写死的 `C:\Users\CQYDDD\.local\bin\python3.12.exe`，改为自动探测本机 Python 3.11（依次尝试 `where python3.11` → `py -3.11` → PATH 中的 `python`，且校验版本必须为 3.11.x）；探测失败时给出明确报错提示，不再要求每个成员手改脚本。
- `backend/start.bat`：启动命令 `python3.12 -m uvicorn ...` 改为 `python3.11`。
- `README.md`：环境要求由 "Python 3.12（写死路径）" 改为 "Python 3.11（start.bat 自动探测）"。
- `docs/团队交接与开发指南.md`、`docs/团队协作开发手册.md`：所有 `python3.12` 命令示例与环境要求统一改为 `python3.11` / 3.11（推荐）。

## 二、数据库与课程资源（真实资源替换占位数据）

- 问题定位：`backend/app/data/` 下缺少正式数据库 `course_agent.db`（仅存备份 `course_agent.db.bak.final_delivery`），直接启动会由种子数据生成 12 条无真实文件的演示资源（R101–R112）。
- 从 `.bak.final_delivery` 恢复生成 `backend/app/data/course_agent.db`（含 65 条真实本地资源、9 章 34 知识点图谱、课程元信息），原备份文件保留未动。
- URL 迁移：资源 URL 由旧中文目录 `/assets/resources/数据结构1-9/{教学视频|教材文献|课堂课件PPT}/` 批量改写为现目录 `/assets/resources/data-structures-1-9/{videos|textbooks|slides}/`，共 65 条，已逐一核对磁盘文件全部存在。
- 验证：以 Python 3.11 启动后端，`/api/v1/student/resources` 返回 65 条；视频 / PPT / PDF / 封面四类静态文件经 HTTP 访问均为 200。

## 三、后端（资源导入脚本健壮性）

- `backend/import_course_resources.py`：
  - 头部说明 `python3.12` 改为 `python3.11`；
  - 修复 `SOURCE_DIR == TARGET_DIR` 时 `shutil.copy2` 抛 `SameFileError` 的问题（同名文件跳过复制），保证脚本在当前目录结构下可重复执行。

## 四、硬编码绝对路径排查（跨设备兼容）

- 全项目扫描 `C:\Users\...`、`D:\...`、`/home/` 等个人机器路径：仅 `start.bat` 一处（已在上面修复），后端 Python 代码与前端页面均使用相对路径定位（`Path(__file__)` 推导项目根），无需改动。
- 结论：项目已无上一任开发者设备相关的硬编码路径；媒体工具 `media_utils.py` 中的 `C:/Windows/Fonts/...` 为 Windows 系统字体目录（带多候选与降级处理），属合理系统路径，保留。

---

## 记录规范（后续改动请遵守）

1. 按块追加：**后端**、**前端渲染**、**LLM/Agent 相关**、**启动脚本·文档**、**数据库·数据**、其他自行补块；
2. 每条格式：`- 文件/模块：改动内容（动机）`，一行一条；
3. 新改动追加到对应块的末尾，并注明日期；
4. 破坏性改动（删表、改接口签名、改数据结构）需单独加粗标注。
