# course-agent 课程资源收集清单

> 用途：对照系统真实数据结构，列出接入 `course-agent` 需要**去寻找/准备**的原始课程资源。
> 每张清单项标明：对应系统落点 / 建议格式 / 去哪找 / 优先级。带 `[ ]` 的条目可逐项勾选收集。
> 系统课程示例：数据结构与算法（course_id=`C2026DS001`，约 28 个知识点 `KP01`–`KP73+`）。

---

## 一、总览（资源类别 → 系统落点 → 优先级）

| # | 资源类别 | 系统落点 | 优先级 |
|---|---------|---------|-------|
| A | 课程基础信息 | `Course` 表 | **P0** |
| B | 知识点体系（大纲拆解） | `GraphNode` + `KpDetail` | **P0** |
| C | 课堂课件 PPT | `Resource(type=ppt)` | **P0/P1** |
| D | 教学视频 / 录课 | `Resource(type=video)` | P1 |
| E | 教材与拓展文献 | `Resource(type=doc)` | P1 |
| F | 习题 / 题库 | `Resource(type=quiz)` + `Question` 表 | **P0** |
| G | 班级与学生名单 | `users` / `ClassInfo` | P1（真实部署） |
| H | 历次作答记录 | `answer_records` / `practice_sessions` | P2 |
| I | AI 答疑预置语料 | `ai.py` 的 `_ANSWER_BANK` | P2（可选） |
| J | 课件原文（RAG 语料） | 向量库（需新增） | 可选增强 |
| K | 能力目标 / 干预 / 报告模板 | `goal` 边 / `InterventionTemplate` / `Report` | P2 |

---

## 二、详细清单

### A. 课程基础信息 　**[P0]**
- [ ] 课程名称（如「数据结构与算法」）
- [ ] 课程代码（如 `CS20301`）
- [ ] 开课学期（如 2026 春季）
- [ ] 授课教师姓名
- [ ] 学分、总章节数、知识点总数估算
- → 落点 `Course` 表，决定前端页头与统计基数。

### B. 知识点体系（大纲拆解） 　**[P0] — 最关键，必须先定**
- [ ] 完整课程大纲 / 教学进度表（按章节）
- [ ] 每个知识点：名称、所属章节、难度(1–5)、是否重点、学时
- [ ] 重点知识点的一句话摘要（用于详情页 `summary`）
- [ ] 知识点先修/后继关系（如「图的存储结构 → Dijkstra」）
- [ ] 知识点关联的生活化问题（如「如何为地图导航设计最优路线？」）
- → 落点 `GraphNode`(KPxx) + `KpDetail`。这是知识图谱、学习驾驶舱、练习出题的共同基础。

### C. 课堂课件 PPT 　**[P0/P1]**
- [ ] 每一章/节的课堂 PPT（`.pptx`）
  - 需记录：标题、对应章节/知识点(KPxx)、页数
  - 对齐示例：`R104`「第4章 树与二叉树 课堂课件」68 页
- [ ] 课件版本与日期（避免混入旧版）
- → 落点 `Resource(type=ppt)`。若要做 AI 真溯源，还需能提取 PPT 文本（见 J）。

### D. 教学视频 / 录课 　**[P1]**
- [ ] MOOC / 课堂录课视频（`.mp4` 或链接）
  - 需：标题、时长（`分:秒`，如 `24:18`）、对应知识点
  - 对齐示例：`R107`「Dijkstra 算法原理与手工模拟」24:18
- [ ] 补救 / 错题微课（短视频）
- → 落点 `Resource(type=video)`，前端详情页展示学习进度。

### E. 教材与拓展文献 　**[P1]**
- [ ] 主教材信息（书名、作者、章节页码，如《数据结构（C语言版）》第 6 章）
- [ ] 拓展文献（如《算法导论》相关章）
  - 需：书名、页码范围、对应知识点
- → 落点 `Resource(type=doc)`。AI 答疑的 citations 常引用这些出处。

### F. 习题 / 题库 　**[P0] — 工作量最大**
- [ ] 章节自测题（每章约 20 题）
- [ ] 综合训练包（如图论 35 题）
- [ ] 题目原始文档（Word/Excel/题库导出），每题需含：
  - [ ] 题干（支持 HTML，如用 `<code>` 标注代码/数字；注意 `<` `>` 要转义）
  - [ ] 选项（单选/多选/判断/填空/代码）
  - [ ] 正确答案
  - [ ] 解析
  - [ ] 难度(1–5)、分值
  - [ ] 归属知识点(KPxx)
  - [ ] 知识点路径、错误类型（概念混淆/公式记忆错误/算法流程不清/计算失误/指针操作不清）
  - [ ] 班级正确率、平均用时（若有历史）
- → 落点 `Resource(type=quiz, count)` + `Question` 表（逐题结构化）。

### G. 班级与学生名单 　**[P1，真实部署必须]**
- [ ] 班级列表（如 `CL2301`）与班主任
- [ ] 学生名单：学号、姓名、班级、账号、初始密码
- → 落点 `users` / `ClassInfo` / `TeacherClass`。Demo 可用 seed 假数据，真实上线须替换。

### H. 历次作答记录 　**[P2]**
- [ ] 学生历次作业/测验成绩与作答明细
- → 落点 `answer_records` / `practice_sessions`，支撑掌握度、错题分析、预警。无则系统用 seed 模拟。

### I. AI 答疑预置语料（可选） 　**[P2]**
- [ ] 高频学生问题清单
- [ ] 每问的参考答案（HTML）+ 引用出处（课件名/页码/原文 quote）
- → 落点 `ai.py` 的 `_ANSWER_BANK`（关键词→答案）。系统已内置 Dijkstra/遍历/循环队列/哈夫曼 4 类，可补充更多。

### J. 课件原文 / 视频字幕（RAG 语料，可选增强） 　**[可选]**
- [ ] PPT 可提取文本（或另存 `.txt`/`.md` 讲义）
- [ ] 教材 PDF 文本
- [ ] 视频字幕/讲稿（`.srt`/`.txt`）
- → 用于真正的 AI 溯源：切片 → 向量化 → 检索。这是让 AI「读懂」课件的前提（当前系统未实现）。

### K. 能力目标 / 干预模板 / 报告模板 　**[P2]**
- [ ] 课程能力目标（对应 `goal` 类图谱边）
- [ ] 教学干预策略模板（如「针对薄弱点推送补救微课」）
- [ ] 学情报告模板
- → 落点 `GraphLink(goal)` / `InterventionTemplate` / `Report`。

---

## 三、原始资源交付格式建议
- **课件/视频/教材**：原始文件（`.pptx`/`.mp4`/`.pdf`）+ 一张「资源登记表」（见下）
- **知识点**：Excel/表格（列：KP编号, 名称, 章节, 难度, 是否重点, 学时, 先修, 摘要）
- **习题**：Excel（列：题号, 知识点, 题型, 难度, 题干, 选项A~D, 答案, 解析, 错误类型）或题库导出 JSON
- **班级/学生**：Excel（学号, 姓名, 班级, 账号, 密码）

## 四、资源登记表（直接复制填写）
```
资源编号 | 标题 | 类型(ppt/video/doc/quiz) | 关联知识点KP | 章节 | 页数/时长/题数 | 来源 | 文件位置
R101 | 第4章 树与二叉树 课堂课件 | ppt | KP31 | 第4章 | 68页 | 课堂PPT | D:\课件\ch4.pptx
```

## 五、收集顺序建议
1. **P0**：A 课程信息 → B 知识点 → F 习题（系统能跑、题能练）
2. **P1**：C 课件 → E 教材 → D 视频 → G 班级（资源列表/详情丰富）
3. **P2**：H 作答记录 → I 答疑语料 → K 模板
4. **可选**：J 课件原文（立项做 RAG 时再收）

> 提示：清单 B（知识点）和 F（习题）是「让系统能用」的硬门槛；C/D/E（课件音视频文档）是「让资源列表好看」的软资产；J（课件原文）是「让 AI 真智能」的前置条件，可最后考虑。
