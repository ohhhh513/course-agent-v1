# 课程智能体系统 · 文档导航

> 本目录收录「课程智能体系统」的专项文档，目标是**让协作者无需通读源码即可上手与对接**。
> 根目录 `README.md` 提供项目总览与启动方式；`相较v1.0的改动.md` 记录全部变更历史。

---

## 阅读顺序建议

| 顺序 | 文档 | 读者 | 解决什么问题 |
| --- | --- | --- | --- |
| **0** | **项目真实交互梳理.md** | **所有人（必读）** | ★ 各模块「真实实现 / 空转 / 占位」逐项清单、现存 bug 与隐患。**接手第一份该读的文档** |
| 1 | 接口文档.md | 后端 / 前端（必读） | 全部 API 的 Path / Method / 请求 / 响应契约，含**实现状态标注**（✅真实 / ⚠️空数据 / 🚧占位） |
| 2 | 数据模型与字段说明.md | 后端 / 前端（必读） | 每个接口 `data` 的字段结构、前端强依赖字段路径、SSE 事件字段 |
| 3 | 页面与功能清单.md | 后端 / 产品 | 每个页面需要哪些数据（页面 ↔ 接口映射） |
| 4 | 鉴权与会话方案.md | 后端 / 前端 | 登录、JWT、路由守卫、找回密码、User 结构 |
| 5 | 智能体工作流说明.md | Agent / 后端 | 智能答疑与智能出题的完整工作流、工具链、SSE 事件序列 |
| 6 | 前端架构与运行.md | 前端 / 任何人 | 技术栈、目录、模块职责、本地运行 |
| 7 | 团队协作开发手册.md | 所有人 | 数据口径、数据模型、协作流程、红线与技术债、数据库基准同步 |

---

## 关键事实速查

- **技术栈**：原生 HTML/CSS/JS + ECharts，无框架、无构建。
- **数据来源**：所有取数走 `API.*`；`api.js` 中 `config.mode = 'http'`，**默认直连真实后端**（`/api/v1`）。
- **统一信封**：`{ code, message, data, traceId }`，成功 `code=0`，前端读 `data`。
- **鉴权**：`Authorization: Bearer <JWT>`；登录 `/auth/login`，当前用户 `/auth/profile`。
- **角色**：`student`（`student.html`，7 视图）/ `teacher`（`teacher.html`，7 视图）。
- **首页**：`index.html` = 登录页；`forgot.html` 找回密码；`home.html` 产品介绍（可选）。
- **Mock 已下线**：早期 `assets/js/mock/data.js` 与 `mode:'mock'` 已移除；`api.js` 中的 mock 分支为死代码，**切勿切回**。

### 演示账号（后端种子）

| 用户名 | 密码 | 角色 |
| --- | --- | --- |
| `student` | `123456` | 学生 · 陈思远 |
| `teacher` | `123456` | 教师 · 李文博 |

---

## 两件"接手必知"

1. **AI 已经接好了**：智能答疑（`/ai/chat/stream`）与智能出题（`/question/gen`）调用的是**真实 LLM**（DeepSeek + RAG），当前为 `live` 模式。想知道运行模式，访问 `GET /api/v1/agent/status` 看 `mode` 字段。
2. **"学情类"功能显示为 0 不是 bug**：`learning_paths` 表 408 条全部为 `todo`、`answer_records` 仅 2 条，导致掌握率 / 预警 / 归因 / 报告**算法正确但没有输入数据**。补齐数据即可，详见 `项目真实交互梳理.md`。

---

## 文档一致性约定

- **实时权威接口文档**：<http://127.0.0.1:8000/docs>（FastAPI Swagger，自动生成、永远与代码同步）。
- 接口契约以 `接口文档.md` 为准，字段结构以 `数据模型与字段说明.md` 为准。
- **文档与代码冲突时，以代码 + `项目真实交互梳理.md` 为准**，并回头修正文档。
- 相对 v1.0 的任何改动（含文档变更）必须记入根目录 `相较v1.0的改动.md`。

---

> 接口规模：约 130 个端点，按 11 个模块组织：`auth` / `course` / `graph` / `student` / `practice` / `ai` / `teacher` / `analysis` / `question` / `intervention` + `report` / `agent`。
