# 课程智能体系统 · 接口文档（API v1）

> 适用范围：学生端与教师端前端（`course-agent/`）。
> 前端 `assets/js/api.js` 已固定 `config.mode='http'`，页面只通过 `API.*` 取数并**直连真实后端** `/api/v1`。（早期 `mock` 模式与 `assets/js/mock/data.js` 已下线。）
> **实时权威文档**：<http://127.0.0.1:8000/docs>（FastAPI Swagger 自动生成，永远与代码同步）。本文档提供契约说明与**实现状态标注**。

**状态标注说明**（本次新增，对应 `项目真实交互梳理.md`）：

| 标记 | 含义 |
| --- | --- |
| ✅ | 真实实现（真实读写数据库 / 真实算法 / 真实 LLM 调用） |
| ⚠️ | 真实实现，但当前受空数据影响输出为空或 0 |
| 🚧 | 占位 / 空壳（返回值硬编码或不落库） |

---

## 1. 约定

### 1.1 基础信息

| 项 | 值 |
| --- | --- |
| Base URL | `/api/v1` |
| 字符集 | UTF-8，`Content-Type: application/json`（文件上传用 `multipart/form-data`） |
| 鉴权 | `Authorization: Bearer <JWT>`，由 `POST /auth/login` 返回 |
| 请求约定 | `GET` 参数拼 query；`POST/PUT/DELETE` 用 JSON body |

### 1.2 统一响应信封

所有接口返回如下结构，前端统一读取 `data`：

```json
{ "code": 0, "message": "success", "data": { }, "traceId": "svc-xxxx" }
```

- `code === 0` 成功；`code !== 0` 为业务失败，前端展示 `message`（中文）。
- 全局异常处理器在 `main.py` 兜底：**HTTP 仍返回 200**，信封 `code=500`。
- ⚠️ 受保护接口 token 缺失/失效时返回 **HTTP 401**（`{"detail": "未登录或 token 缺失"}`），不走信封。

### 1.3 错误文案（前端据此提示）

| 场景 | message |
| --- | --- |
| 登录账号不存在 | `账号不存在` |
| 密码错误 | `密码错误，请重试` |
| 新密码过短（<6） | `新密码至少 6 位` |
| 其他异常 | 返回可读中文 `message`，前端 `Toast` 直接展示 |

### 1.4 列表分页

列表类接口统一返回 `{ total, list: [...] }`，支持 `page`（从 1 起）/ `size`。

> **特例**：`GET /teacher/classes` 返回**裸数组** `[{classId,name}]`（非 `{list}`），前端已兼容。**新增接口请勿混用两种形态。**

### 1.5 身份来源

身份（userId / role）由后端从 JWT 解析，**不要**依赖前端在 query/body 重复传 userId。

---

## 2. 认证与公共 `/auth`、`/course`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| POST | `/auth/login` | ✅ | 账号密码登录，返回 token + user；学生登录顺便写当日打卡 |
| GET | `/auth/profile` | ✅ | 当前用户（教师额外带 `classes`） |
| POST | `/auth/logout` | 🚧 | **占位**：仅返回成功，不吊销 token（前端清本地会话） |
| POST | `/auth/reset-password` | ✅ | 重置密码，**要求已登录且只能改本人** |
| GET | `/course/{courseId}` | ✅ | 课程基本信息（默认 `C2026DS001`）⚠️ 返回的统计数字是种子演示常数，与真实库不符 |

**登录**
```jsonc
// request
{ "username": "student", "password": "123456", "role": "student" }
// response.data
{ "token": "<JWT>", "user": { /* 见 §13 User */ } }
```
> `role` 仅用于前端预选角色标签，后端以数据库中的真实角色为准。

**找回密码**
```jsonc
// request
{ "username": "student", "password": "newPass123" }
// response.data
{ "ok": true }
```
> ⚠️ 当前后端要求**已登录**且只能重置**当前登录账号**；公开页 `forgot.html` 的「免登录找回」尚未接入，正式环境需叠加验证码/安全问题。

---

## 3. 三大课程图谱 `/graph`（师生共用）

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/graph?courseId=&type=knowledge\|problem\|goal&userId?` | ✅ | 查询图谱（三类图谱均有真实数据） |
| GET | `/graph/kp/{kpId}` | ✅ | 知识点详情（含前后置、挂载资源、习题统计） |
| GET | `/graph/path` | ✅ | 基于前后置关系的推荐学习路径 |

```jsonc
// GET /graph response.data → Graph
{ "graphType": "knowledge",
  "categories": [ { "name": "已掌握", "color": "#22c55e" } ],
  "nodes": [ { "id":"KP01", "name":"算法与复杂度", "category":0, "mastery":92, "difficulty":2, "isKey":true, "hours":4 } ],
  "links": [ { "source":"KP01", "target":"KP02", "relation":"pre" } ] }
```
> `mastery` 由 `answer_records` **实时计算**（非种子静态值）。⚠️ 当前答题记录极少，多数节点 mastery 为 0。

---

## 4. 学生端 `/student`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/student/dashboard` | ✅ ⚠️ | 学习驾驶舱聚合（概览/指标/待办/薄弱点/猜你想问/最近动态） |
| GET | `/student/resources?type=&kpId=&keyword=&page=&size=` | ✅ | 资源中心列表 |
| POST | `/student/resources/{resId}/view` | ✅ | 记录资源查看次数 |
| GET | `/student/resources/{resId}/progress` | ✅ | 读取学习进度 |
| POST | `/student/resources/{resId}/progress` | ✅ | 保存进度（**取最大值，不回退**） |
| GET | `/student/resource-stats` | ✅ | 资源学习进度统计 |
| GET | `/student/mastery/matrix` | ⚠️ | 知识点掌握矩阵（按章分组） |
| GET | `/student/ability/radar` | ⚠️ | 能力雷达（基线 80/75 为硬编码常量） |
| GET | `/student/growth?dimension=week\|month` | ⚠️ | 成长轨迹（依赖打卡/答题数据） |
| GET | `/student/compare` | ⚠️ | 与班级平均对比 |
| GET | `/student/alerts?level=&status=` | ⚠️ | 我的预警（当前 `alerts` 表为 0 行） |
| PUT | `/student/alerts/{alertId}/read` | ✅ | 标记预警已读 |
| GET | `/student/messages` | ⚠️ | 教师私信 / 系统通知（当前 `messages` 表为 0 行） |
| PUT | `/student/messages/{msgId}/read` | ✅ | 标记消息已读 |

**驾驶舱结构**
```jsonc
// response.data
{ "overview": { "courseProgress", "currentNode": {...}, "streakDays", "todayStudyMinutes", "status" },
  "coreMetrics": { "completionRate", "masteryRate", "goalAchieveRate", "updatedAt" },
  "todos": [ ... ], "weakPoints": [ ... ], "suggestedQuestions": [ ... ], "recentActivities": [ ... ] }
```

---

## 5. 智能练习 `/practice`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/practice/modes` | ✅ | 练习模式列表（含推荐题量） |
| POST | `/practice/sessions` | ✅ | 创建练习会话（组卷） |
| GET | `/practice/sessions/current?mode=` | ✅ | 当前进行中的练习存档（无则 `null`） |
| GET | `/practice/sessions/{sessionId}/questions` | ✅ | 取题 |
| POST | `/practice/answers` | ✅ | 提交单题 → 即时判分 + 解析 + 知识点定位树，并写 `answer_records`、触发预警重算 |
| POST | `/practice/sessions/{sessionId}/finish` | ✅ | 结束练习，返回练习报告 |
| GET | `/practice/wrong-book?mastered=&errorType=&page=` | ✅ | 错题本 |
| GET | `/practice/wrong-book/{qId}/detail` | ✅ | 错题详情（含相似题、推荐资源、图题规格） |
| DELETE | `/practice/wrong-book/{qId}` | ✅ | 移出错题本（标记已掌握） |

**组卷请求**
```jsonc
{ "mode": "weak|order|random|wrong", "kpIds": ["KP52"], "count": 10, "difficulty": 3 }
```
**答题请求 / 响应**
```jsonc
// request
{ "sessionId": "PS...", "qId": "KHD047", "answer": "B", "durationSeconds": 42 }
// response.data
{ "qId", "correct", "rightAnswer", "analysis", "kpPath", "classCorrectRate", "avgSeconds", "masteryDelta" }
```

---

## 6. AI 智能答疑 `/ai`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/ai/sessions` | ✅ | 历史会话列表（复用 `chat_sessions`，`flow_id='explain'`） |
| GET | `/ai/sessions/{sessionId}/messages` | ✅ | 会话消息（含溯源 citations） |
| DELETE | `/ai/sessions/{sessionId}` | ✅ | 删除会话（仅限本人，含全部消息） |
| GET | `/ai/suggest-questions` | 🚧 | **恒返回空**：查询的 `teacher_class_data` 类型从未被写入 |
| POST | `/ai/chat` | ✅ | 一次性返回完整回答（信封） |
| POST | `/ai/chat/stream` | ✅ | **SSE 流式**（前端实际使用） |
| POST | `/ai/feedback` | 🚧 | **TODO 空实现**，直接返回 `{accepted:true}` |

**提问请求**（`/ai/chat` 与 `/ai/chat/stream` 同）
```jsonc
{ "sessionId": "new", "question": "Dijkstra 为什么不能处理负权边？", "method": "guided", "kpId": "KP52", "courseId": "C2026DS001" }
```
**`/ai/chat` 响应**
```jsonc
{ "messageId": "MSG...", "sessionId": "CS...", "method": "guided",
  "content": "<p>...</p>", "citations": [ { "source", "locator", "quote", "kp" } ],
  "outOfScope": false, "sourceCount": 2 }
```
> **严格溯源约束**：提问超出课程材料范围时返回 `outOfScope:true` 与降级文案，不输出推测性答案。
> **`/ai/chat/stream` 的 SSE 事件协议**见 §10。

---

## 7. 教师端 `/teacher`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/teacher/classes` | ✅ | 当前教师所带班级 → **裸数组** `[{classId,name}]` |
| GET | `/teacher/dashboard?classId=` | ✅ ⚠️ | 教学驾驶舱（`delta*` 字段在无对比数据时返回 `null`） |
| GET | `/teacher/heatmap?classId=&dimension=` | ⚠️ | 知识点 × 学生 掌握热力图 |
| GET | `/teacher/students?classId=&alertLevel=&keyword=&sortBy=&page=` | ✅ | 学生列表 |
| GET | `/teacher/students/{userId}/profile` | ⚠️ | 个体学情详情 |
| GET | `/teacher/alerts?classId=&level=&status=&type=&kpId=&page=` | ⚠️ | 预警列表（当前为空） |
| PUT | `/teacher/alerts/{alertId}/review` | ✅ | 人工复核：`{action:'confirm\|ignore\|annotate', note?}` |
| POST | `/teacher/messages` | ✅ | 向学生私信：`{userId, content}` |
| GET | `/teacher/resources?keyword=` | ✅ | 教师资源管理列表 |
| GET | `/teacher/resources/kps` | ✅ | 上传可选知识点（按章节分组） |
| POST | `/teacher/resources/upload` | ✅ | 上传资源（`multipart/form-data`：`file,title,kp,category`） |
| DELETE | `/teacher/resources/{resId}` | ✅ | 删除资源 |

**热力图结构**
```jsonc
{ "kpAxis": [ {"kpId","name"} ], "studentAxis": [ {"userId","name"} ],
  "data": [ [kpIndex, studentIndex, masteryRate] ], "weakest": [ ... ] }
```

---

## 8. 归因与错题分析 `/analysis`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/analysis/errors?classId=&chapter=&kpId=&timeRange=` | ⚠️ | 错题与归因分析（`mainWrongOption` 用启发式给 B/C） |
| GET | `/analysis/weak-chain?classId=&kpId=` | ⚠️ | 知识点关联薄弱链路（前端**未调用**） |
| GET | `/analysis/causes` | ⚠️ | 前端文案为「AI 归因建议」，实现实为 `GROUP BY error_type`（前端**未调用**） |

> ⚠️ 三个接口都真实统计 `answer_records`，但该表仅 2 条记录，输出必然接近空。

---

## 9. AI 出题与题库管理 `/question`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/question/gen/config` | ✅ | 出题配置（真实 KP 列表 + 题型选项） |
| POST | `/question/materials` | 🚧 | **占位**：返回 `{fileId,status:"parsing",progress:0}`，不落盘不解析 |
| POST | `/question/gen` | ✅ | **SSE 流式 AI 出题**（真实 LLM，`generate_items` 流） |
| POST | `/question/gen/stream` | ✅ | `/gen` 的语义化别名，行为一致 |
| GET | `/question/drafts?status=` | ✅ | 我的出题草稿箱 |
| PUT | `/question/drafts/{draftId}` | ✅ | 编辑草稿（保存时后端重新校验） |
| DELETE | `/question/drafts/{draftId}` | ✅ | 删除草稿 |
| POST | `/question/drafts/{draftId}/publish` | ✅ | 发布草稿 → 正式题库（`questions`） |
| GET | `/question/bank?kpId=&type=&status=&difficulty=&keyword=&page=` | ✅ | 题库列表 |
| PUT | `/question/{qId}` | ✅ | 编辑习题 |
| POST | `/question/review` | ✅ | 批量审核发布：`{qIds[],action:'approve\|reject\|publish'}` |
| DELETE | `/question/{qId}` | ✅ | 删除习题 |
| POST | `/question/import` | ✅ | 批量导入（JSON 数组） |
| POST | `/question/import/file` | ✅ ⚠️ | 上传文件导入（**真实解析 docx/pdf/图片**）；但前端 `question.js:558` 取错 token，UI 上必然 **401** |
| POST | `/question/packs` | 🚧 | **占位**：只返回假 `{packId, pushed:true}`，不生成也不落库 |

> **【破坏性变更】** `/question/gen` 已由「非流式返回 JSON 题目数组」改为「**SSE 流式事件流**」，前端 `question.js` 已同步切换。事件协议见 §10。
> **草稿隔离**：生成的题目**只写入 `st_question_drafts`（按 user_id 归属个人）**，绝不自动进入正式题库；必须经 `/question/drafts/{id}/publish` 人工发布。

---

## 10. SSE 流式事件协议

### 10.1 智能答疑 `/ai/chat/stream`

```
meta(sessionId,messageId) → tool_start / tool_end → think(模型推理) → content(逐 token) → citations → done(outOfScope)
```

| event | data |
| --- | --- |
| `meta` | `{ sessionId, messageId }` |
| `tool_start` / `tool_end` | `{ name, ... }` |
| `think` | `{ delta }`（deepseek `reasoning_content`，供思考面板） |
| `content` | `{ delta }`（逐 token） |
| `citations` | `{ items: [ {source,locator,quote,kp} ] }` |
| `done` | `{ outOfScope, demo }` |
| `error` | `{ message }` |

### 10.2 智能出题 `/question/gen`

```
meta(batchId,count) → tool_start / tool_end → draft(每题落库即推) → done(batchId,draftIds,demo,count)
```

| event | data |
| --- | --- |
| `meta` | `{ batchId, count }` |
| `draft` | `{ draft_id, status, errors, payload }` |
| `log` | 过程事件（原 `text` 类型） |
| `done` | `{ batchId, draftIds, demo, count }` |
| `error` | `{ message }` |

> `demo: true` 表示当前未配置 `LLM_API_KEY`，处于演示模式（出题会复制同章原题骨架）。

---

## 11. 智能体运维 `/agent`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/agent/status` | ✅ | 运行模式（`live`/`demo`）、模型名、embedding 模型、可用 flows |
| GET | `/agent/ingest/stats` | ✅ | RAG 切片统计（仅教师）：`{chunks, embeddingModels, activeEmbeddingModel}` |
| POST | `/agent/ingest/bank` | ✅ | 重建题库索引（仅教师，幂等，运维入口） |
| POST | `/agent/ingest/file` | ✅ | 上传补充资料入 RAG 库（仅教师，支持 json/pdf/pptx/txt/md） |

> 想知道 AI 到底跑的是 live 还是 demo，直接看 `GET /agent/status` 的 `mode`。

---

## 12. 干预与报告 `/intervention`、`/report`

| 方法 | 路径 | 状态 | 说明 |
| --- | --- | --- | --- |
| GET | `/intervention/list?classId=&status=&scope=` | ✅ | 干预建议列表（表为空时**按规则真实生成**） |
| POST | `/intervention/{ivId}/confirm` | ✅ | 确认/自定义后执行：`{steps?,resources?,note?}` |
| POST | `/intervention/{ivId}/reject` | ✅ | 忽略建议 |
| GET | `/intervention/{ivId}/effect` | ⚠️ | 干预前后对比曲线（对照组序列为伪造 `rate+8`） |
| GET | `/intervention/templates?scene=&keyword=` | ⚠️ | 策略库模板（`successRate`/`avgLift` 为编造公式） |
| POST | `/intervention/templates` | ✅ | 沉淀新策略模板 |
| GET | `/report/list?classId=&page=` | ✅ | 历史报告归档 |
| POST | `/report/generate` | ✅ ⚠️ | 一键生成报告（结构真实，因上游数据空而内容空） |
| GET | `/report/{reportId}` | ✅ | 报告详情（多章节 sections） |
| POST | `/report/{reportId}/export` | ✅ | 导出：`{format:'pdf'\|'html'}` → 二进制下载 |

---

## 13. 数据模型速查

| 实体 | 关键字段 |
| --- | --- |
| User | userId, username, name, role, studentNo, className, title, dept, classes[] |
| GraphNode | id, name, chapter, category, mastery, difficulty, isKey, hours |
| GraphLink | source, target, relation(`pre/advance/parallel/split/map/error/support`) |
| Heatmap | kpAxis[], studentAxis[], data[[kpIdx,stuIdx,val]], weakest[] |
| Alert | alertId, level(`red/yellow/green`), student, kp, type, trigger, trendData[], status(`open/reviewed/ignored`) |
| Question | qId, type, difficulty, status(`pending/approved/published/archived`), stem, options[], answer, analysis, kpPath[], figure_json, has_image |
| 出题草稿 | draftId, status, createdAt, errors[], payload{id,chapter,question,options,answer,analysis} |
| Intervention | ivId, status(`pending/running/done`), level, scope(`common/individual`), title, target, reason, steps[], expectEffect |
| ReportSection | title, paragraphs[], bullets[] |

> 完整字段与前端强依赖路径见 `数据模型与字段说明.md`。枚举与格式约定（时间、百分比、颜色）见同文档。

---

## 14. 前端对接说明

- 所有取数统一走 `assets/js/api.js` 的 `API.<module>.<method>(payload)`；**不要绕过它直接 `fetch`**（会拿不到自动注入的 JWT）。
- `mode='http'` 直连真实后端，页面零改动；`mock` 模式已下线，请勿切回。
- 可解释性：AI 答疑、AI 出题均返回 `citations`/`sourceRef`，前端以折叠卡片展示原文溯源；未命中课程材料时按「严格溯源」约束降级。
- 实时性：预警从产生到触达控制在分钟级。
